{
	"scripts": {
		"test": "lerna exec --parallel -- npm t",
		"lint": "lerna exec --parallel -- npm run lint",
		"upload-coverage": "lerna exec --parallel -- npm run upload-coverage",
		"lerna": "lerna",
		"ncu": "lerna exec --parallel -- ncu -u",
		"b-common": "lerna exec --parallel -- npm run b-common"
	},
	"devDependencies": {
		"husky": "^2.4.0",
		"lerna": "^3.14.1"
	},
	"husky": {
		"hooks": {
			"pre-commit": "npm run lint",
			"pre-push": "npm t"
		}
	}
}
