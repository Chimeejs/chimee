{
	"scripts": {
		"test": "lerna exec --parallel -- npm t",
		"lint": "lerna exec --parallel -- npm run lint",
		"upload-coverage": "lerna exec --parallel -- npm run upload-coverage",
		"lerna": "lerna",
		"ncu": "lerna exec --parallel -- ncu -u",
		"build": "lerna exec -- npm run build",
		"publish": "lerna publish from-package --dist-tag alpha"
	},
	"devDependencies": {
		"@types/jest": "^24.0.13",
		"husky": "^2.4.0",
		"lerna": "^3.14.1",
		"ts-jest": "^24.0.2"
	},
	"husky": {
		"hooks": {
			"pre-commit": "npm run lint",
			"pre-push": "npm t"
		}
	}
}
